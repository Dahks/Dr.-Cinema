import { StyleSheet } from "react-native";
import { baccent, dark, darkest, white } from "./colors";

export default StyleSheet.create({
  border: {
    borderWidth: 1,
    borderColor: baccent,
    borderStyle: "solid",
  },
});
